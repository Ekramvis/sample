%h1
  Edit
  = resource_class.name

= render 'form'

.destruction
  %p You can also delete this #{ resource_class.name.downcase }. This is an un-reversible operation.
  = link_to 'Destroy', resource_url, method: :delete, :class => 'btn btn-danger', data: { confirm: 'Are you sure?' }
